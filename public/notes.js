//indexOf() method searches the array for the specified item, and returns it's 
// position. The search starts at the specified position, and if no position is 
// specified, it starts at the beginning. It returns -1, if no item is found. 

lastIndexOf() //if we want to start from end to beginning. 

array.indexOf(item, start) //syntax; start is optional

array.push() // method adds one or more elements to the end 
//of an array and returns the new length of arr

array.splice (start) // start is the index at which to start changing the array
//this returns an array containing the deleted elements

